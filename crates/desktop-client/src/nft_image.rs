//! Desktop re-export of the canonical deterministic NFT image generator.
//!
//! The implementation lives in `xrpl-vault-crypto-core` so desktop, tests,
//! and future mobile clients use the same visual/metadata pipeline.

pub use xrpl_vault_crypto_core::nft_image::*;
